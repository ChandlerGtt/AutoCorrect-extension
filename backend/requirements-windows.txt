# Minimal requirements for Windows (pre-built wheels only)
# Use this if you're on Windows and getting build errors

# Core Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Machine Learning & NLP (pre-built wheels available)
transformers>=4.36.0
torch>=2.0.0
sentencepiece>=0.1.99

# Data Handling (pre-built wheels)
numpy>=1.24.0
requests>=2.31.0
tqdm>=4.66.0

# Caching (pure Python, no compilation)
diskcache>=5.6.0

# Utilities (pure Python)
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Note: Removed packages that require compilation on Windows:
# - spacy (requires C++ compiler)
# - cryptography (requires Rust/C compiler)
# - pandas (can have build issues)
# - accelerate (optional, can install later if needed)

# If you need these packages, install them after:
# pip install spacy pandas cryptography
